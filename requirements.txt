# core
rich>=13.7
pydantic>=1.10,<3
requests>=2.31

# local model support (for --provider local)
transformers>=4.41.0
sentencepiece>=0.1.99
# PyTorch: install appropriate build for your system.
# If pip install fails, follow https://pytorch.org/get-started/locally/
torch>=2.1.0
